name: Build Website

on:
    push:
        paths:
            - "web/**"

jobs:
    server:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: actions/setup-node@v1
              with:
                  node-version: 12
            - run: cd web && yarn && yarn build
